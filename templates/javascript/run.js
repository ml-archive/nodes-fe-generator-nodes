(function() {
	'use strict';

	/**
	 * @ngdoc overview
	 * @name <%= appname %>
	 * @description
	 * # <%= appname %>
	 *
	 * Main module of the application.
	 */
	angular
		.module('<%= appname %>')
		.run(run);

	function run($state, $rootScope) {

	}
})();
